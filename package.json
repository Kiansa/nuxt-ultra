{
  "name": "nuxt-ultra",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "nuxt dev --dotenv .env.local",
    "build": "nuxt build --dotenv .env.production",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare --dotenv .env.production",
    "lint": "eslint .",
    "typecheck": "nuxt typecheck",
    "upnuxt": "npx nuxt upgrade",
    "setup": "node setup.js"
  },
  "dependencies": {
    "nuxt": "^4.4.5"
  },
  "devDependencies": {
  }
}
